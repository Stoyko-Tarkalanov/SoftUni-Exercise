function solve(input) {
    let n = Number(input[0]);
    let min = 10000000

    for (let i = 1; i <= n; i++) {
        let current = Number(input[i]);

        if (current < ) {

            console.log();
        }
    }

}
solve(["2", "100", "99"]);